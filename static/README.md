# WorkfxHUB 静态版

纯 HTML + CSS + JS，**无需构建、无需重启**。改完文件直接刷新浏览器即可看到效果。

## 使用方式

**方式一：直接打开**（最简单）
```
在浏览器中打开 index.html
```

**方式二：本地服务器**
```bash
npm run static
```
访问 http://localhost:3002

## 文件说明

- `index.html` - 页面结构
- `styles.css` - 样式
- `app.js` - 交互逻辑

修改任一文件后，保存并刷新浏览器即可，无需任何构建步骤。
